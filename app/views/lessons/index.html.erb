<p><%= link_to "Add a new lesson", new_lesson_path %></p>

<h1>Lessons</h1>

<ul>
  <% @lessons.each do |lesson| %>
    <li>
      <%= link_to lesson.name, lesson_path(lesson) %>
      <small><%= link_to "Edit", edit_lesson_path(lesson) %></small>
    </li>
  <% end %>

</ul>
